keytool -list -keystore ../tmp/myKeystore

keytool -genkey -keystore ../tmp/myKeystore -alias Guillaume

keytool -list -keystore ../tmp/myKeystore

#jarsigner -keystore myKeystore ../dist/gui.jar myself
